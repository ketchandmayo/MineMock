# Bind address
IP=0.0.0.0
PORT=25565

# Message shown when client tries to join (login disconnect)
ERROR=Сервер временно недоступен. Попробуйте позже.
# You can also pass raw JSON chat component, e.g. {"text":"Соединение потеряно"}

# Server list (status ping) fields
MOTD=§6MineMock §7| §fДобро пожаловать!
VERSION_NAME=1.20.1
# Optional: overrides protocol autodetection from VERSION_NAME
# PROTOCOL=763
MAX_PLAYERS=100
ONLINE_PLAYERS=7
